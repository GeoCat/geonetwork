import { AggregationTranslatePipe } from './aggregation-translate-pipe';

describe('AggregationTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new AggregationTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
